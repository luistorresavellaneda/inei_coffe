$(document).ready(function (e) {
    //jQuery()
    /*
    Seleccion por Elemento
    $('*') --> seleccionar cualquier elemnto del DOM
    $('div') --> por una etiqueta
    $('div,p,span') --> se puede llamar multiples etiquetas seperador po coma
    $('#id-elemento') --> acceder con ID
    $('.clase')
    $('a[rel]')
    $('a[rel="insumos"]')

    Seleccion por Winget
    $("input:text")
    $("input:checkbox")
    $(":button")

    $(".container > a")
    $(".container:not(.active)")

    $('.container:not(data-categoria="'+id+'")')
    */
});